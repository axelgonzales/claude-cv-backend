spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/cv_db
    username: cv_user
    password: Axel2024Secure!
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

server:
  port: 8080

app:
  firebase:
    service-account: classpath:firebase-service-account.json
  cors:
    allowed-origins: http://localhost:5173
